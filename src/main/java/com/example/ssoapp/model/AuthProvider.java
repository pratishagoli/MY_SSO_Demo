package com.example.ssoapp.model;

/**
 * This enum is now public and in its own file,
 * so it can be accessed by any other package,
 * including your 'service' package.
 */
public enum AuthProvider {
    LOCAL,
    //GOOGLE,
    MINIORANGE
}